/**
 * @param {HTMLElement} rootElement
 * @param {ArtistsChartOptions} [options]
 */
export const init = (rootElement, options) => {
}

/**
 * @typedef {Object} ArtistsChartOptions
 * @property {(artist: string) => void}
 */
