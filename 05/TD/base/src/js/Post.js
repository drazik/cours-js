/**
 * @param {PostData} post
 * @returns {HTMLElement}
 */
export const create = (data) => {
  // TODO
}

/**
 * @typedef {Object} PostData
 * @property {number} id
 * @property {string} title
 * @property {body} body
 */
