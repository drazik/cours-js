/**
 * @param {Artist} artist
 * @returns {HTMLElement}
 */
export const make = (artist) => {
  // Créer un élément permettant d'afficher les données de l'artist
  // Voir l'énoncer pour avoir un aperçu de la structure attendue
}

/**
 * @typedef {Object} Artist
 * @property {string} name
 * @property {Tag[]} tags
 * @property {Biography} bio
 */

/**
 * @typedef {Object} Tag
 * @property {string} name
 * @property {string} url
 */

/**
 * @typedef {Object} Biography
 * @property {string} summary
 */
