// Importer les composants Result et SearchForm

/**
 * @param {HTMLElement} container
 * @returns {App}
 */
export const init = (container) => {
  // Initialiser un composant Result et un composant SearchForm

  const destroy = () => {
    // Détruire l'instance de Result et l'instance de SearchForm
  }

  return { destroy }
}

/**
 * @typedef {Object} App
 * @property {Function} destroy
 */
