// Récupérer l'élément ayant la classe "gallery" dans le DOM

// Initialiser un composant Gallery avec cet élément

// Récupérer l'élément ayant la classe "alerter" dans le DOM

// Initialiser un composant Alerter avec cet élément

// Récupérer l'élément ayant la classe "search-form" dans le DOM

// Initialiser un composant SearchForm avec cet élément, et utiliser les
// options  `onLoading, `onSucces` et `onError` pour faire communiquer les
// différents composants ensemble
