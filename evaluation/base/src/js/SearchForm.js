/**
 * @typedef {Object} Options
 * @property {(result: import("./api").QueryResult) => void} [onSuccess]
 * @property {() => void} [onLoading]
 * @property {(error: any) => void} [onError]
 */

/**
 * @param {HTMLFormElement} root
 * @param {Options} [options]
 */
export const init = (root, options = {}) => {
  // TODO
}
