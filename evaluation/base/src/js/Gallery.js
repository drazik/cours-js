/**
 * @param {HTMLElement} root
 */
export const init = (root) => {
  /**
   * @param {import("./api").QueryResult} result
   */
  const showImages = (result) => {
    // TODO
  }

  return { showImages }
}
